import { parse } from '@babel/parser';

// parse js file
// @unitless-io:test
export const parseJs = (code) => {
  return parse(code);
};

// parse ts file
export const parseTs = (code) => {
  return parse(code);
};
