import { parse } from '@babel/parser';

// @unitless-io:test
export const parseJs = (code: string) => {
  return parse(code);
};

// @unitless-io:test
export const parseTs = (code: string) => {
  return parse(code);
};
